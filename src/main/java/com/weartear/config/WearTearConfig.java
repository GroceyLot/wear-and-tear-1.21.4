package com.weartear.config;

public class WearTearConfig {

    // How many ticks between decrements. 1200 = 60 seconds
    public static int timeBetweenDecrements = 1200;

    // How many times a player needs to "stand" on a block before it converts
    public static int timeToConvert = 10;

    // In a real mod, you could load these from a .json config file
    // or other data source during mod initialization.
}
